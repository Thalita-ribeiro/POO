package lutador;

public class Lutadora extends Lutador {

    public Lutadora(String nome, String nacionalidade, int idade,
                    double altura, double peso, int vitoria,
                    int derrotas, int empates) {
        super(nome, nacionalidade, idade, altura, peso, vitoria, derrotas, empates);
    }
}

